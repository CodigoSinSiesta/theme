/**
 * @codigosinsiesta/theme/composables — barrel re-export.
 *
 * Composables runes-based (Svelte 5). Importables compactos:
 *   import { useIntersectionObserver } from '@codigosinsiesta/theme/composables';
 *
 * También reexportados desde el root del paquete:
 *   import { useIntersectionObserver } from '@codigosinsiesta/theme';
 */

export { useIntersectionObserver } from './useIntersectionObserver.svelte.js';
export { useCounterAnimation, easeOutCubic } from './useCounterAnimation.svelte.js';
